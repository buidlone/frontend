import styled, { css, keyframes } from "styled-components";

export const InvestigateIcon = styled.span`
  position: absolute;
  font-size: 40px;
  color: #ffbc0d;
  top: 50%;
  left: 28.5%;
  z-index: 1000;
  cursor: pointer;
`;
