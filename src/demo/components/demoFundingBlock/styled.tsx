import styled, { css } from "styled-components";
import {
  BlockWrapper,
  FContainer,
} from "../../../components/fundingBlock/styled";

export const DemoBlockWrapper = styled(BlockWrapper)`
  height: 22.688rem;
`;

export const DemoFContainer = styled(FContainer)`
  padding-top: 0;
  gap: 10%;
  justify-content: center;
`;
